
<p>
<h3><%= @question.title %></h3>
</p>

<p>
  Asked <%= time_ago_in_words(@question.updated_at) %> ago.
</p>
  <% unless @feed.empty? %>
    <%=pluralize(@feed.count, "answer") %> so far:
    <table>
      <%= render "shared/feed"  %>
    </table>
  <%else %>
    This question has no anwsers no far.
  <%end %>


